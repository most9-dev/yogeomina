# 라이브 주문서 설치 가이드

전체 순서: **① 구글 시트 만들기 → ② 스크립트 붙여넣기 → ③ 웹 앱 배포 → ④ 주문서에 연결 → ⑤ 인터넷에 올리기**

①~③은 사장님 구글 계정에서 직접 해야 하는 부분이에요 (약 10분).
④~⑤는 URL만 알려주시면 Claude가 해드릴 수 있어요.

---

## ① 구글 시트 만들기

1. [sheets.google.com](https://sheets.google.com) 접속 → **빈 스프레드시트** 새로 만들기
2. 파일 이름을 `라이브 주문 관리` 등으로 변경
   (탭은 다음 단계에서 자동으로 만들어지니 그대로 두세요)

## ② 스크립트 붙여넣기

1. 시트 상단 메뉴에서 **확장 프로그램 → Apps Script** 클릭
2. 열린 화면의 코드를 전부 지우고, `apps-script.gs` 파일의 내용을 **통째로 붙여넣기**
3. 💾 저장 (Ctrl+S / Cmd+S)
4. 상단 함수 선택 드롭다운에서 **`setup`** 선택 → **▶ 실행** 클릭
   - 처음 실행하면 권한 요청 창이 떠요 → **권한 검토 → 내 계정 선택 →
     "고급" → "안전하지 않은 페이지로 이동" → 허용**
     (내가 만든 스크립트라 뜨는 안내일 뿐, 안전합니다)
5. 시트로 돌아가 보면 **[상품목록]** / **[주문접수]** 탭이 자동으로 생겨 있어요

## ③ 웹 앱으로 배포

1. Apps Script 화면 오른쪽 위 **배포 → 새 배포** 클릭
2. ⚙️(톱니바퀴) → **웹 앱** 선택
3. 설정:
   - 다음 사용자 인증 정보로 실행: **나**
   - 액세스 권한이 있는 사용자: **모든 사용자** ← 중요!
4. **배포** 클릭 → 나오는 **웹 앱 URL 복사**
   (`https://script.google.com/macros/s/…/exec` 형태)

> ⚠️ 나중에 스크립트를 수정하면 **배포 → 배포 관리 → ✏️ → 새 버전 → 배포**를 해야 반영돼요.

## ④ 주문서에 연결

`index.html` 맨 위 `CONFIG` 부분을 수정합니다 (Claude에게 URL을 주면 대신 해드려요):

```
SHOP_NAME: "샵 이름",
SCRIPT_URL: "③에서 복사한 웹 앱 URL",
BANK: "은행명 계좌번호 (예금주)",
KAKAO_CHANNEL_URL: "https://pf.kakao.com/_xxxxx/chat",
SHIP_FEE: 4000,
```

## ⑤ 인터넷에 올리기 (GitHub Pages, 무료)

1. [github.com](https://github.com) 가입 (무료)
2. **New repository** → 이름을 `주문서용`으로 아무거나 (예: `order`) → **Public** → 생성
3. **uploading an existing file** 링크 클릭 → `index.html` 업로드 → Commit
4. 저장소 **Settings → Pages → Branch: main → Save**
5. 1~2분 뒤 `https://아이디.github.io/order/` 주소가 생겨요

이 주소를 **카톡 채널 자동응답 + 유튜브 고정댓글**에 넣으면 끝!

---

## 평소 운영 방법

- **라이브 전**: [상품목록] 탭에 상품 추가/수정 → 폼에 자동 반영 (저장만 하면 됨)
- **품절**: 그 상품 줄을 삭제하거나 상품번호를 지우면 검색 안 됨
- **주문 확인**: [주문접수] 탭에 실시간으로 쌓임. 입금 확인하면 "입금확인" 칸을 "완료"로 변경
- **새 주문 알림 받기**: 시트에서 도구 → 알림 설정 → "사용자가 양식을 제출할 때"는 폼 전용이라 안 되고,
  **변경사항이 있을 때 이메일 알림**으로 설정하면 주문이 들어올 때 메일이 와요
